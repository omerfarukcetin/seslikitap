[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing için - React Router kullanıyorsanız gerekli
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
